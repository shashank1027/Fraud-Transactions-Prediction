# Fraud-Transactions-Prediction
This project is a custom Isolation Forest fraud detection model trained end-to-end on AWS SageMaker. I cleaned the raw dataset into parquet, pushed it to Amazon S3, and triggered SageMaker to run train.py inside a container — not using any built-in algorithm.  
